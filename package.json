{
  "name": "telegramcode",
  "version": "2.0.0",
  "license": "MIT",
  "author": "Sergei Solo",
  "description": "Telegram forum-supergroup bot for Claude Code / OpenCode — one topic per task, isolated sessions, per-thread MCP",
  "keywords": [
    "claude-code",
    "opencode",
    "telegram-bot",
    "ai-agents",
    "vibe-coding",
    "remote-development",
    "tmux"
  ],
  "files": [
    "dist/",
    "README.md",
    "LICENSE",
    ".env.example",
    "examples/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/olosegres/telegramcode.git"
  },
  "homepage": "https://github.com/olosegres/telegramcode#readme",
  "bugs": {
    "url": "https://github.com/olosegres/telegramcode/issues"
  },
  "main": "dist/cli.js",
  "bin": {
    "telegramCode": "./dist/cli.js",
    "telegramcode": "./dist/cli.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js dist/index.js",
    "build:watch": "tsc -w",
    "start": "node dist/cli.js",
    "dev": "tsx watch src/cli.ts",
    "hot": "node dist/cli.js hot",
    "install-link": "yarn install && yarn build && npm link",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "yarn typecheck && yarn build",
    "preversion": "yarn typecheck && yarn build && yarn test",
    "test": "node --import tsx --import ./src/__tests__/globalTestSetup.ts --test 'src/__tests__/**/*.test.ts'",
    "claude": "UID=$(id -u) GID=$(id -g) docker compose run --rm claude"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "croner": "^10.0.1",
    "dotenv": "^16.4.5",
    "form-data": "^4.0.5",
    "node-pty": "^1.0.0",
    "telegraf": "^4.16.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "nodemon": "^3.1.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "yarn@4.9.2"
}
