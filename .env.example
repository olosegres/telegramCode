# For development of telegram-code itself
# Not needed when using telegram-code in another project

# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=

# Your Telegram user ID (required, get from @userinfobot)
ALLOWED_USERS=123456789

# Work directory (default: /workspace)
WORK_DIR=/workspace

# Default agent backend: claude | opencode (default: claude)
DEFAULT_AGENT=claude

# OpenCode server (for opencode adapter)
OPENCODE_URL=http://localhost:4096
OPENCODE_PASSWORD=
OPENCODE_USERNAME=opencode

# Custom opencode binary path (optional, for using fork)
# OPENCODE_BIN=/path/to/opencode
